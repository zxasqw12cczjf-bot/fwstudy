---
title: SwiftUI 的状态管理
tags: [iOS, SwiftUI]
---

请简述 `@State`、`@Binding`、`@Observable` 的区别和适用场景。

---

## 答案

### @State
用于视图内部的状态管理，视图拥有数据的所有权。适用于简单的本地状态。

### @Binding
用于父视图传递给子视图的绑定，子视图可以修改但数据所有权在父视图。用于需要子视图修改父视图数据的场景。

### @Observable
用于跨多个视图共享的可观察对象，适用于复杂的应用状态。iOS 17+ 推荐使用的新方式。